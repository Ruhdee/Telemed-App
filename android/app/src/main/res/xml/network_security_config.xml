<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 
        Allow cleartext (HTTP) traffic for local development.
        For production, use HTTPS and remove this config.
    -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- Allow all local/private IP ranges -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator localhost -->
        <domain includeSubdomains="true">10.0.2.2</domain>
        
        <!-- Localhost -->
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        
        <!-- Your specific IP -->
        <domain includeSubdomains="true">10.67.176.82</domain>
        
        <!-- Common private IP ranges - add more as needed -->
        <domain includeSubdomains="true">192.168.1.105</domain>
        
        <!-- Wildcard patterns (commented as they require regex which Android doesn't support) -->
        <!-- If your IP changes frequently, manually add new IPs here -->
    </domain-config>
</network-security-config>
